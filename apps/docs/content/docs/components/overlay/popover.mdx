---
title: Popover 弹出层
description: 在当前触发器附近展示非模态辅助内容。
---
import { PopoverExample } from "../../../../components/examples/popover/basic"

<ComponentPreview title="发布前检查"><PopoverExample /></ComponentPreview>
<SourceCode code={"import { Popover, PopoverContent, PopoverTrigger } from '@chenyibo111/ui'"} />
## API
<ApiTable rows={[{name:"open / onOpenChange",type:"boolean / callback",defaultValue:"undefined",description:"控制弹出状态。"}]} />
## 可访问性
- 内容必须能通过键盘到达，Escape 可关闭；关键错误不应只放在这里。
