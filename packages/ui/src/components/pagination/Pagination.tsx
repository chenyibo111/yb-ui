import { useYBLocale } from "../../provider/YBProvider"
export type PaginationProps={page:number;pageCount:number;onPageChange:(page:number)=>void;siblingCount?:number;disabled?:boolean}
export function Pagination({page,pageCount,onPageChange,disabled=false}:PaginationProps){const l=useYBLocale();return <nav aria-label="分页"><button aria-label={l.pagination.previous} disabled={disabled||page<=1} onClick={()=>onPageChange(page-1)}>{l.pagination.previous}</button>{Array.from({length:pageCount},(_,i)=>i+1).map(n=><button aria-current={n===page?"page":undefined} aria-label={l.pagination.page(n)} disabled={disabled} key={n} onClick={()=>onPageChange(n)}>{n}</button>)}<button aria-label={l.pagination.next} disabled={disabled||page>=pageCount} onClick={()=>onPageChange(page+1)}>{l.pagination.next}</button></nav>}
