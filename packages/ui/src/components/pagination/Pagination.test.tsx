import { render,screen } from "@testing-library/react"; import userEvent from "@testing-library/user-event"; import { describe,expect,it,vi } from "vitest"; import { Pagination } from "./Pagination"
describe("Pagination",()=>{it("marks the current page and changes pages",async()=>{const u=userEvent.setup(),onPageChange=vi.fn();render(<Pagination onPageChange={onPageChange} page={2} pageCount={5}/>);expect(screen.getByRole("button",{name:"第 2 页"})).toHaveAttribute("aria-current","page");await u.click(screen.getByRole("button",{name:"下一页"}));expect(onPageChange).toHaveBeenCalledWith(3)})})
