import { render,screen } from "@testing-library/react"; import userEvent from "@testing-library/user-event"; import { describe,expect,it } from "vitest"; import { YBProvider } from "../../provider/YBProvider"; import { DatePicker } from "./DatePicker"
describe("DatePicker",()=>{it("uses provider copy and closes on Escape",async()=>{const u=userEvent.setup();render(<YBProvider locale="zh-CN"><DatePicker mode="range"/></YBProvider>);const trigger=screen.getByRole("button",{name:/选择日期范围/});await u.click(trigger);await u.keyboard("{Escape}");expect(screen.queryByRole("dialog")).not.toBeInTheDocument()})})
