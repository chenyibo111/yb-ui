import { render, screen } from "@testing-library/react"; import userEvent from "@testing-library/user-event"; import { describe,expect,it } from "vitest"; import { Tabs,TabsContent,TabsList,TabsTrigger } from "./Tabs"
describe("Tabs",()=>{it("activates tabs with arrow keys",async()=>{const u=userEvent.setup();render(<Tabs defaultValue="a"><TabsList><TabsTrigger value="a">概览</TabsTrigger><TabsTrigger value="b">设置</TabsTrigger></TabsList><TabsContent value="a">概览内容</TabsContent><TabsContent value="b">设置内容</TabsContent></Tabs>);await u.tab();await u.keyboard("{ArrowRight}");expect(screen.getByRole("tab",{name:"设置"})).toHaveAttribute("data-state","active")} )})
